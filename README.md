# landingcatala — ACat

Web estàtica (landing) de l'app **ACat** per aprendre català a Andorra.

## Desplegament (Cloudflare Pages)
Lloc **100% estàtic**: no cal cap comanda de build.
- **Build command:** *(buit)*
- **Build output directory:** `/` (arrel)

Els fitxers són `index.html` i la carpeta `assets/`.
